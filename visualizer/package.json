{
  "name": "votify-visualizer",
  "version": "0.1.0",
  "private": true,
  "description": "Visualizador didático em tempo real para o fluxo interno do Votify.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5174",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5174"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.3.0"
  }
}
